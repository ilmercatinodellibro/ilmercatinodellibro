query getBooks($page: Int!, $rows: Int!, $filter: String) {
  books(page: $page, rows: $rows, filter: $filter) {
    id
    isbnCode
    subject
    authorsFullName
    title
    originalPrice
    publisherName
  }
}

mutation loadBooksIntoDatabase {
  loadBooksIntoDatabase
}
