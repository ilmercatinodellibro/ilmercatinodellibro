query getBooks($input: BookQueryPayload!) {
  books(input: $input) {
    id
    isbnCode
    subject
    authorsFullName
    title
    originalPrice
  }
}

mutation loadBooksIntoDatabase {
  loadBooksIntoDatabase
}
