defaults
auth on
tls ${TLS_ENABLED}
tls_starttls ${TLS_STARTTLS}
tls_trust_file ${TLS_TRUST_FILE}
logfile /logs/email.log

account default
host ${MAIL_HOST}
port ${MAIL_PORT}
from ${MAIL_FROM_DEFAULT}
user ${MAIL_USER}
password ${MAIL_PASS}