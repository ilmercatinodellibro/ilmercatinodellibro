defaults
auth on
tls on
tls_trust_file /etc/ssl/certs/ca-certificates.crt
logfile /logs/email.log

account default
host ${MAIL_HOST}
port ${MAIL_PORT}
from ${MAIL_FROM_DEFAULT}
user ${MAIL_USER}
password ${MAIL_PASS}